on: [push, pull_request]

name: Ubuntu build test
jobs:
  tests:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v3
      - uses: cachix/install-nix-action@v18
      - uses: cachix/cachix-action@v12
        with:
          name: konradmalik
          authToken: "${{ secrets.CACHIX_AUTH_TOKEN }}"

      - name: Flake check
        run: |
          nix flake check

      - name: Shell build linux
        run: |
          nix build --no-link .#devShells.x86_64-linux.default

      - name: Flake build generic linux
        run: |
          nix build --no-link .#homeConfigurations.konrad@linux.activationPackage

      - name: Flake build nixos m3800
        run: |
          nix build --no-link .#nixosConfigurations.m3800.config.system.build.toplevel

      - name: Flake build nixos vaio
        run: |
          nix build --no-link .#nixosConfigurations.m3800.config.system.build.toplevel

      - name: Flake build nixos xps12
        run: |
          nix build --no-link .#nixosConfigurations.m3800.config.system.build.toplevel
